spring.application.name=Eureka_Server
server.port=8761

# Important: the server does not register itself
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false
eureka.instance.prefer-ip-address=true